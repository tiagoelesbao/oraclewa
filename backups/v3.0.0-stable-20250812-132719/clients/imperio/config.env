# ================================================
# CONFIGURAÇÃO ISOLADA - CLIENTE IMPÉRIO
# ================================================

# Identificação do Cliente
CLIENT_ID=imperio
CLIENT_NAME="Império Prêmios"
SERVICE_TYPE=all  # all, recovery, broadcast

# ================================================
# SERVIÇO: RECUPERAÇÃO
# ================================================
RECOVERY_ENABLED=true
RECOVERY_PORT=4001
RECOVERY_INSTANCES=imperio1,imperio2

# Database Recuperação (Isolado)
RECOVERY_DB_HOST=localhost
RECOVERY_DB_PORT=5432
RECOVERY_DB_NAME=imperio_recovery
RECOVERY_DB_USER=imperio_user
RECOVERY_DB_PASS=ImperioRecovery2024@Secure

# Redis Recuperação (Isolado)
RECOVERY_REDIS_HOST=localhost
RECOVERY_REDIS_PORT=6379
RECOVERY_REDIS_DB=0

# ================================================
# SERVIÇO: BROADCAST (ISOLADO!)
# ================================================
BROADCAST_ENABLED=true
BROADCAST_PORT=4002
BROADCAST_ISOLATED=true  # CRÍTICO: Roda separado
BROADCAST_INSTANCES=broadcast-imperio-1,broadcast-imperio-2,broadcast-imperio-3

# Database Broadcast (Isolado)
BROADCAST_DB_HOST=localhost
BROADCAST_DB_PORT=5432
BROADCAST_DB_NAME=imperio_broadcast
BROADCAST_DB_USER=imperio_user
BROADCAST_DB_PASS=ImperioBroadcast2024@Secure

# Redis Broadcast (Isolado)
BROADCAST_REDIS_HOST=localhost
BROADCAST_REDIS_PORT=6380  # Porta diferente!
BROADCAST_REDIS_DB=0

# Anti-ban Configuration (Conti Chips)
ANTIBAN_ENABLED=true
ANTIBAN_STRATEGY=conti_chips
ANTIBAN_DELAY_MIN=30
ANTIBAN_DELAY_MAX=120
ANTIBAN_BATCH_SIZE=10
ANTIBAN_PAUSE_MIN=60
ANTIBAN_PAUSE_MAX=180

# Limites Broadcast
BROADCAST_DAILY_LIMIT=1000
BROADCAST_HOURLY_LIMIT=100
BROADCAST_PER_INSTANCE_LIMIT=350

# Warmup de Chips
CHIP_WARMUP_DAY1_MIN=10
CHIP_WARMUP_DAY1_MAX=20
CHIP_WARMUP_DAY2_MIN=30
CHIP_WARMUP_DAY2_MAX=40
CHIP_WARMUP_DAY3_MIN=50
CHIP_WARMUP_DAY3_MAX=60
CHIP_WARMUP_MATURE_MIN=70
CHIP_WARMUP_MATURE_MAX=100

# ================================================
# PROVIDER CONFIGURATION
# ================================================
# Provider Principal (evolution-baileys = gratuito, z-api = R$99/instância)
PRIMARY_PROVIDER=evolution-baileys

# Evolution API (Baileys - Gratuito)
EVOLUTION_API_URL=http://128.140.7.154:8080
EVOLUTION_API_KEY=Imperio2024@EvolutionSecure
EVOLUTION_PROVIDER_TYPE=baileys

# Z-API Configuration (Premium - R$99/instância)
# Descomente e configure se quiser usar Z-API para recursos avançados
# PRIMARY_PROVIDER=z-api
# ZAPI_CLIENT_TOKEN=seu_token_aqui
# ZAPI_BASE_URL=https://api.z-api.io
# ZAPI_INSTANCE_TOKEN=token_da_instancia

# ================================================
# SEGURANÇA
# ================================================
WEBHOOK_TOKEN=Imperio2024SecureWebhook
API_KEY=sk-imperio-7h8k9m2n3p4q5r6s
JWT_SECRET=imperio-jwt-secret-2024

# ================================================
# LOGGING
# ================================================
LOG_LEVEL=info
LOG_DIR=/var/log/imperio
LOG_PREFIX=[IMPERIO]
LOG_RETENTION_DAYS=30

# ================================================
# MONITORAMENTO
# ================================================
METRICS_ENABLED=true
METRICS_PORT=9091
HEALTH_CHECK_ENABLED=true
HEALTH_CHECK_INTERVAL=30000

# ================================================
# BUSINESS RULES
# ================================================
WORKING_HOURS_ENABLED=false  # Temporariamente desabilitado
WORKING_HOURS_START=08:00
WORKING_HOURS_END=22:00
WORKING_HOURS_TIMEZONE=America/Sao_Paulo

# ================================================
# FEATURE FLAGS
# ================================================
FEATURE_INTERACTIVE_BUTTONS=true
FEATURE_TEMPLATE_VARIATIONS=true
FEATURE_AUTO_RESPONSE=true
FEATURE_COMMUNITY_INTEGRATION=true
FEATURE_ADVANCED_ANALYTICS=true